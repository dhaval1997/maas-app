# Set this to your Neon (or other Postgres) connection string.
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Keep mock off when using real backend DB.
NEXT_PUBLIC_USE_MOCK_API=false

# Optional for separate API host deployments. Keep empty for same-origin Next.js APIs.
NEXT_PUBLIC_API_URL=

